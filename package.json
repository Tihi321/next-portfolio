{
  "scripts": {
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "production-start": "npm install -f && npm run build && npm run start"
  },
  "dependencies": {
    "@zeit/next-sass": "^1.0.1",
    "next": "^9.0.3",
    "node-sass": "^4.12.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0"
  }
}
