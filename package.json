{
  "scripts": {
    "deve": "cross-env NEXT_ENV=development next",
    "dev": "next",
    "build": "next build",
    "start": "next start",
    "production-start": "cross-env NEXT_ENV=production npm install -f && npm run build && npm run start"
  },
  "dependencies": {
    "@zeit/next-sass": "^1.0.1",
    "isomorphic-fetch": "^2.2.1",
    "micro-cors": "^0.1.1",
    "next": "^9.0.3",
    "node-sass": "^4.12.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-use": "^10.6.0"
  },
  "devDependencies": {
    "autoprefixer": "^9.6.1",
    "cross-env": "^5.2.0"
  }
}
